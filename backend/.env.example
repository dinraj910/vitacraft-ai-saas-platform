# ── LLM Provider API Keys (multi-provider fallback) ───────────
# Primary — Groq (fastest, get from console.groq.com)
GROQ_API_KEY="gsk_..."

# Fallback 1 — Google Gemini (aistudio.google.com)
GEMINI_API_KEY="AIza..."

# Fallback 2 — Cohere (dashboard.cohere.com)
COHERE_API_KEY="..."

# Fallback 3 — HuggingFace (huggingface.co/settings/tokens)
HF_API_KEY="hf_..."

# ── Database ──────────────────────────────────────────────────
DATABASE_URL="postgresql://user:password@localhost:5432/vitacraft"

# ── Auth ──────────────────────────────────────────────────────
JWT_ACCESS_SECRET="your-access-secret"
JWT_REFRESH_SECRET="your-refresh-secret"

# ── AWS S3 ────────────────────────────────────────────────────
AWS_ACCESS_KEY_ID=""
AWS_SECRET_ACCESS_KEY=""
AWS_REGION="us-east-1"
AWS_S3_BUCKET="vitacraft-ai-files"

# ── Misc ─────────────────────────────────────────────────────
PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# ── Stripe (Test Mode) ──────────────────────────────────────
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# Stripe Price IDs (create in Stripe Dashboard → Products)
STRIPE_PRICE_FREE="price_free_placeholder"
STRIPE_PRICE_PRO="price_..."
STRIPE_PRICE_ENTERPRISE="price_..."
